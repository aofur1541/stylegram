<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.spring.flea.dao.FleaMapper">
	<!-- <select id="selectRowCount" parameterType="map" resultType="integer">
		SELECT COUNT(*)
			FROM profleaboard b JOIN profleainfo i USING (fb_num)
			<where>
					<if test="filter == 'top'">
						fb_topcheck=1
					</if>
					<if test="filter == 'bottom'">
						fb_bottomcheck=1
					</if>
					<if test="filter == 'hat'">
						fb_hatcheck=1
					</if>
					<if test="filter == 'shoes'">
						fb_shoescheck=1
					</if>
				</where>
	</select> -->
	<select id="selectList" parameterType="map" resultType="fleaVO">
		SELECT rownum, a.* 
			FROM (SELECT * 
				FROM profleaboard b JOIN profleainfo i USING (fb_num) ORDER BY fb_num desc)a 
				<where>
					<if test="filter == 'top'">
						fb_topcheck=1 AND rownum BETWEEN 1 AND 5
					</if>
					<if test="filter == 'bottom'">
						fb_bottomcheck=1 AND rownum BETWEEN 1 AND 5
					</if>
					<if test="filter == 'hat'">
						fb_hatcheck=1 AND rownum BETWEEN 1 AND 5
					</if>
					<if test="filter == 'shoes'">
						fb_shoescheck=1 AND rownum BETWEEN 1 AND 5
					</if>
					<if test="filter == ''">
						rownum BETWEEN 1 AND 5
					</if>
				</where>
	</select>
	<update id="update" parameterType="fleaVO">
		UPDATE profleaboard SET 
			fb_title=#{fb_title},
		<if test="fb_filename != ''">
			fb_photo=#{fb_photo},
			fb_filename=${fb_filename},
		</if>
			fb_price=#{fb_price},
			fb_content=#{fb_content},
			fb_modifydate=sysdate
		 WHERE fb_num=#{fb_num}
	</update>
</mapper>